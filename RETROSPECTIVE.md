# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: RETROSPECTIVE ネクストアクションの消化とスキル手順の構造化（#109 / #110）

### 何をしたか

前サイクルの振り返りで挙げた 2 つのネクストアクションを、いずれも「prose の努力目標」から「必ず発火するスキル手順」へ構造化して消化した。#109 では機能変更時の locale ドキュメント同期規約を `/implement` 締めの既存 SPEC.md 同期規約の直下へ 1 行追加（PR #111）。#110 では `/health-check` の定期運用を、必ずサイクル終了時に発火する `/retrospective` の起点（新ステップ 1「整合検査」）へ組み込んだ（PR #112）。本サイクルの `/retrospective` は、その新ステップ 1 の初回ドッグフーディングを兼ねる。

---

### よかったこと

**新設ステップ 1 が初回実行で即座に実 drift を検出**
`/retrospective` に組み込んだ `/health-check` 整合検査を初めて回した結果、`commands/launch_history.rs`（#93 新設の起動履歴サブシステム）が SPEC.md §3.2 モジュール表とルート CLAUDE.md ツリーの双方から漏れている drift を検出した。仕組みを作った当のサイクルで、それが自力で価値を出した = 費用対効果の即時実証。

**「prose → 必ず発火する手順」への構造化パターン**
#109・#110 とも、努力目標（CLAUDE.md の緩い記述）が発火漏れした反省を、既に必ず通る経路（既存の同期規約／必ず走る `/retrospective`）へ相乗りさせて解決した。新しい表・機構を足さず既存パターンへ対称配置する形で、KISS・1 in 1 out を保った。

**判断粒度の使い分け**
配置が拮抗した #109 は AskUserQuestion でユーザーに委ね、候補 1 が明確に優位だった #110 は recommend して進めた。決定コストを要否で切り替えた。

---

### 伸びしろ

**モジュール新設時の構造ドキュメント同期に網が無い**
検出した launch_history.rs 漏れは、#93 のサブシステム新設時に SPEC.md §3.2 とルート CLAUDE.md のツリーへの追記が漏れたもの。#109 で作った同期規約は locale キーに限定で、モジュール構成表の同期は含まない。新モジュール追加時の構造ドキュメント同期はまだ規約化されていない。

**docs-only 変更時のコード系ゲート非該当を都度判断していた**
#109・#110 とも `/commit` のコード系チェック（build/test/cargo）を「検証対象が存在しない」として非該当と判断した。同一判断が 2 回出たが、`/commit` には docs-only ケースの明文が無く都度判断だった。「2 回までは許容、3 回目で抽出」の原則からは、次に同型が出たら `/commit` への明文化を検討する（現状 2/3）。

---

### ネクストアクション

- **launch_history.rs のモジュール記述漏れを解消**: SPEC.md §3.2 モジュール表とルート CLAUDE.md ツリーに `commands/launch_history.rs` を追記する（本サイクルの `/health-check` が検出。#103 / #104 と同じく個別 issue 化して追跡）
- **モジュール新設時の構造ドキュメント同期規約の検討**: 上記 drift の根本要因。新モジュール追加時に SPEC.md モジュール表・CLAUDE.md ツリーの同期を促す仕組みを、規約肥大を避けつつ検討する（launch_history.rs 修正 issue に含めるか別立てか判断）
- **docs-only ゲート非該当の 3 回目監視**: 次に docs-only の非該当判断が出たら `/commit` の「追加の確認事項」へ明文化を検討（現状 2/3 で抽出は時期尚早）
