# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: GhostDbActor ＋ 使い捨てスキーマ（#146 / PR #147）

### 何をしたか

#145（scan オフスレッド化）のレビュー対応で露呈した複雑さを 3 つの根源（導出キャッシュへの二重更新規律・規約ベースの排他・揮発キャッシュへの永続機構）に抽象化し、構造的に除去した。ghosts.db への全書き込みを単一 writer アクター（Job enum・専有スレッド）へ集約、sqlx マイグレーション 15 本を CACHE_SCHEMA＋ハッシュ user_version の使い捨てスキーマに置換、JS を SQL 文レベルで読み取り専用化。設計は 4 観点マルチパースペクティブレビューで先に固め、実装は subagent-driven（13 タスク・タスク毎レビュー・最終全ブランチレビュー）で実行。Codex アドバーサリアルレビューも approve。

---

### よかったこと

**設計段階のマルチパースペクティブレビューが High 4 件を実装前に検出**
並行性・SQLite・IPC/移行パス・アドバーサリアルの 4 観点並列レビューが、legacy 履歴移送と全 DROP の順序未定義（永続データ破壊）・リビルドの非原子性・「型が保証」の過大主張・version bump 忘れの silent 破壊クラスを、コードを 1 行も書く前に検出した。特に移送順序は 2 観点が独立にクロス確認しており、多視点の冗長性が確度を上げた。実装後の手戻りに比べ桁違いに安い。

**「計画どおり」を疑うレビュー規約が計画自体の欠陥を 3 度捕捉**
タスクレビューアに「plan-mandated でも欠陥は Important として報告（人間が裁定）」を義務付けた結果、init_cache_schema の単一層化・起動 fail-fast 化の影響・Maintenance の run_guarded 漏れという計画自体の欠陥が拾われ、都度ユーザー裁定または設計書準拠で解決した。計画の権威とレビューの独立性を両立できた。

**parity テストが大規模置換の正しさを注意力から独立させた**
migration 15 本 → CACHE_SCHEMA 1 枚の置換を「旧合成と新 CREATE の完全一致」の二段機械検証（正規化 DDL 全比較＋table_info 構造比較）に落とし、初回一致で通過。「スキーマ初変更時に旧 migrations() ごと削除」という寿命も明記し、検証資産の永住を防いだ。

**SDD ledger がセッション断絶からの正確な復旧を支えた**
Task 3 実装中にセッション上限で実装者が消えたが、ledger＋git log＋コミット済み状態の再検証で、重複作業ゼロで再開できた。「会話の記憶ではなく耐久記録を信じる」運用の実証。

**実装者の逸脱報告文化が機能した**
brief 内の矛盾（migrations 健全性テスト）・brief の数え上げ漏れ（8 箇所目）・fixture rot（固定日付の TTL ドリフト）を実装者が透明に報告し、レビューが妥当性を独立判定する流れが 3 度成立。「黙って従う」でも「黙って逸脱する」でもない中間が回った。

---

### 伸びしろ

**裁定の反映が「追記」に留まり、旧記述の自己矛盾 drift を 2 度生んだ**
fail-fast 裁定を設計書 §2.1 に追記した際、同文書 §4 と SPEC §13 の旧記述（ログのみで続行）を残したため、Task 13 レビューと最終レビューで 2 回 fix になった。根本要因は反映先を「その時見えていたファイル」に限定したこと。→ CLAUDE.md へ「関連語 grep による横断同期」を規約化（抽出済み）。

**計画に書いた絶対値が現実とドリフトした**
brief の固定日付テストが実時計に対して TTL を跨いで腐り（fixture rot）、「呼び出し 7 箇所」は実態 8 箇所だった。いずれも実装者が現場で正しく吸収したが、計画の書き方で防げた。→ CLAUDE.md へ「相対時刻・grep コマンドで指示」を規約化（抽出済み）。

**エディタ診断（stale）と実状態の乖離に毎回検証コストを払った**
サブエージェントの編集途中スナップショットが rust-analyzer 診断として繰り返し届き、都度 bash での再検証を要した。「壊れた・矛盾する出力から推論しない」既存規約どおりの対処で正しく凌げたが、SDD 運用では診断ノイズを既定で疑う心構えが要る（規約追加はせず記録に留める）。

---

### ネクストアクション

- **PR #147 のマージ → `/post-merge-sync`**（CI は clippy ガードステップ込みで初回実走）
- **#148（起票済み）**: final review の別 issue 送り 4 件（clippy open_with_flags 系・record_launch テストの ghosts 側 assert・並行 scan テストの意図コメント・設計書 §9 の検証省略注記）
- **E2E 手動実行の検討**: UI 操作・IPC 契約は不変だが、起動フロー（初回自動リビルド）が変わったため、マージ前後どこかで `/e2e` を一度通すと安心（CI に含まれない唯一の統合テスト）
