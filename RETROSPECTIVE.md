# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: スキル体系再設計（issue #99 / PR #100）

### 何をしたか

Snotra の CLAUDE.md を参考に、開発サイクルの節目と品質検証の観点をスキル化した。ブレインストーミングで要件を一問ずつ確定 → 設計ドキュメント → 全文埋め込みの実装計画 → サブエージェント駆動開発で 12 タスクを実行。新設スキル 8（サイクル系: implement / start-issue / retrospective / deps-update、検証系: ipc-check / symmetry-check / cache-check / health-check）、既存修正 2（commit / pr）、ルート CLAUDE.md の 2 節ポインタ化。全 11 実装タスクがレビュー一発 Approved、最終ブランチレビューは Ready to merge: Yes。

---

### よかったこと

**全文埋め込み計画による転写タスク化**
SKILL.md の完全な本文を計画に埋め込んだことで、実装は「転写 + 検証」に縮退し、最安モデルのサブエージェントで足りた。レビューも「brief との逐語一致」という機械的で確実なゲートになった。判断の余地を計画段階で使い切ることが、実行段階の品質と速度を両立させた。

**レビュアーへの「事実主張の実測検証」指示**
各スキルが参照する節名・CI ステップ名・コマンドの実在を、diff 外の named check として確認させた。ドキュメントの化石化（存在しない節への参照・古いコマンド）を作成時点で阻止できた。

**タスク単位レビュー + 最終ブランチレビューの二段構え**
ポインタ化による参照の逆転（`/commit` の「CLAUDE.md 準拠」が循環参照化）は、タスク単位では正しく見えて全体では矛盾する類の欠陥で、whole-branch レビューだけが検出できた。二段構えは冗長ではなく相補だった。

**進捗台帳による失敗復旧**
最終レビュアーがセッション上限で失敗したが、進捗台帳・brief/report ファイル群が残っていたため、別モデルでの再派遣 1 回で完遂できた。

---

### 伸びしろ

**設計ドキュメントを main に直コミット**
ブランチ戦略のルール自体は CLAUDE.md に既存だったのに、設計フェーズのコミットを「作業」と認識せず実行が漏れた。ブランチ作成 + main リセットで即時是正。今回新設した `/implement`（ステップ 2 のブランチ確認）と `/start-issue` がこの穴の構造的ガードになる。

**計画内のコードフェンス入れ子ミス**
Markdown 成果物（内部に ``` を含む）を計画へ 3 バッククォートで埋め込み、構造が崩れた。セルフレビューで捕捉できたが、埋め込みは最初から外側 4 バッククォートと決めておくべきだった。

**サブエージェント派遣時の書式制約の後出し**
最初の 2 タスクでコミットメッセージに ASCII ピリオドが混入。「句点は全角」を派遣プロンプトに明記した 3 タスク目以降は消滅した。書式制約は最初の派遣から明示する。

---

### ネクストアクション

- **新スキル群のドッグフーディング**: 次のコード変更サイクルで `/start-issue` → `/implement`（検証スキル条件発火）→ `/commit` → `/pr` の全連鎖を実運用し、手順の過不足を洗い出す
- **`/health-check` の初回実行**: ベースラインとして一度回し、SPEC.md・docs・スキルの現状整合を確認する
