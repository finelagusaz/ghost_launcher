# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: bench プロファイルの panic strategy 衝突と、そこから派生した CI の作り直し（PR #207 / #208 / #210 / #212）

### 何をしたか

`cargo bench` を `docs/perf/baseline-100k.md` の手順どおり 2 本連続で回すとビルドが落ちる #201 を修正した。原因は `[lib] crate-type` に `cdylib`/`staticlib` があると cargo が rlib に `-C extra-filename` を付けず、`panic=abort`（bin 経由）と `panic=unwind`（bench target 経由）の 2 unit が同一パスを奪い合うこと。`crate-type = ["rlib"]` へ縮めて解消した（PR #207）。

同 issue が求めた再発検知として bench プロファイルのコンパイルゲートを CI へ追加したが、実測でコストが判明したため 2 サイクル続けて作り直した。PR #210 で path filter ＋ 週次 schedule の別ワークフローへ切り出し、PR #212 で `Swatinem/rust-cache` を両ワークフローへ導入。あわせて較正の規律を CLAUDE.md へ抽出した（PR #208）。

結果、**検知ゲートを 1 つ増やしたうえで、PR の待ち時間は導入前を下回った**（8〜9 分台 → 7m19s）。

---

### よかったこと

**「順序依存」という症状で止まらず、rustc の実引数まで降りた**

issue は「`search_bench` の後に `scan_bench` を回すと落ちる」という順序依存として書かれていた。ここで順序の入れ替えや target 分離という回避策に飛ばず、`cargo bench -v` で rustc の起動引数を読んだ。決め手は `--extern ghost_launcher_lib=...\libghost_launcher_lib.rlib` に**ハッシュ接尾辞が無い**ことで、そこから「2 unit が単一パスを奪い合う競合」という機序に到達できた。**順序依存は競合の見え方であって原因ではない。** 症状の水準で回避策を書いていれば、`cargo test --release` に残る同じ地雷を見落としていた。

**ゲートを「壊れたツリーで落ちること」で較正してから採用した**

新設した CI ゲートが修正前のツリーで決定的に失敗することを 3 つの異なる状態で確認（3/3）してから採用した。ゲートは置いた時点で守れている気になるが、**決して落ちないゲートは機能しているゲートと区別がつかない**。この反転は既存の較正ルールが射程に入れていなかったため、CLAUDE.md へ抽出した（PR #208）。

**コストを推測でなく実測して申し送りに残したことが、次の 2 サイクルを呼んだ**

#207 の時点で CI コストが未知だったため「初回 CI 実行から読み取って追記する」と宣言し、実数（ゲート単体 8m20s）を本文へ残した。直後に #208 が `CLAUDE.md` 1 行の変更で 21m51s かかり、**ゲートが path filter を持たず全 PR に乗っている**ことが可視化された。推測値で「数分程度」と書いていれば異常値は申し送りに乗らず、#209・#211 は生まれていない。**測って書く規律が、次の仕事を呼んだ。**

**効果測定を統制した**

キャッシュの効果測定で、空コミットを積まず `gh run rerun` で同一 SHA を再実行した。ビルド内容が完全に同一で、変わったのはキャッシュの有無だけという比較になる。あわせてベースラインが別 run である限界を明示したうえで、観測された振れ幅（+8s / +26s）に対し効果量が 5 分超で桁が 2 つ違うことを示し、ノイズに帰せられないと論じた。

**設計上の非自明な制約を、実装前に洗い出せた**

GitHub Actions のキャッシュはブランチスコープを持ち、全ブランチが読めるのは既定ブランチの run が作ったものだけ。`ci-bench-profile.yml` は PR・週次・手動しかトリガーを持たなかったため、そのままではキャッシュが PR 間で再利用されない。これを実装前に issue へ書けたため、`push: branches: [main]` の追加が後付けの手当てにならずに済んだ。

---

### 伸びしろ

**「観測が空」を「事実が無い」と読みかけた（本サイクルで 2 回・前サイクルからの再発）**

(1) 新ワークフローに `npm run build` が要るかを調べるため `dist` を退避して `cargo check` を通したが、build script はキャッシュで再実行されておらず観測は無効だった。(2) 環境変数で LTO を落とした状態で `-C panic=abort` が保たれるかを検算しようとしたが、既にビルド済みで rustc 行が 1 つも出ず、grep 0 件を「abort が消えた」と読みかけた。

いずれも「コマンドは成功した」と「検査対象の処理が走った」を同一視したことが根因。**前サイクルの伸びしろ（較正の規律を副次的な計測に適用しそこねた）と同型の再発**であり、規律が「性能計測」という文脈に紐づいていて確認作業一般へ効いていない。→ ルート CLAUDE.md「判断の勘所」へ抽出済み。

**同じシェルの罠を 2 回踏んだ。1 回目のあと記録したのに想起されなかった**

非引用ヒアドキュメント内のバッククォートがコマンド置換される罠で、PR #210 の本文から `` `Cargo.toml` `` 等が黙って消えた。1 回目のあと memory へ記録したが、「gh に渡すとき」と限定して書いたため、次に grep パターンで同じ罠を踏んだときに想起されなかった。**記録の適用範囲を実際の踏み場所より狭く書くと、記録があっても効かない。** → memory の description と適用条件を「Bash へ長文・記号を渡すとき」へ広げた。

**整合検査の計器を粗く書き、誤検出を出した**

`/health-check` の項目 2/3/5（参照パスの実在確認）をアドホックなワンライナーで書いたところ、フルパスでない短縮参照（`cache_schema.rs` 等）を軒並み「実在しない」と誤判定した。basename での解決を足して較正し直したが、**検査器を毎回書き捨てにしている限り、同じ較正を毎サイクル繰り返すことになる。**

**ワークフロー分割の副次効果を設計時に予見していなかった**

#209 の設計時、逐次ステップを別ジョブへ切り出すと待ち時間が「合計」から「max」へ変わることを織り込んでいなかった。結果的に得だったが、予見していれば受け入れ基準に並列化の確認を書けた。分割の効果を時間削減の一次元だけで見積もっていた。

---

### ネクストアクション

- **`rusqlite` の上流追従 — #187 で追跡中**（`tauri-plugin-sql` の sqlx 0.9 対応待ち。解錠条件は未達）
- **`jsdom` 30 / `sha2` 0.11 のメジャー更新 — #198 / #199 で追跡中**
- **main のキャッシュから PR が実際に復元することの実地確認 — 意図的に追跡しない**: 必要条件（main スコープに同一キーのキャッシュが存在すること）は本サイクルで確認済みで、キーはブランチ非依存のため機序上ヒットする。次の PR のログを見れば判明し、外れていればキャッシュが効かないという可視な形で現れる。専用の追跡を置く価値は低い
- **bench ゲートの LTO 無効化による更なる短縮 — 意図的に追跡しない**: `CARGO_PROFILE_BENCH_LTO=false` 等でローカル 285s → 78s になること、`-C panic=abort` が保たれることは実測済み。ただしゲートの守備範囲が「panic strategy の drift」へ狭まり、release プロファイル固有の壊れ方を捕まえなくなる。キャッシュ導入で待ち時間が導入前を下回った現在、この対価を払う動機がない。必要になったら本節が判断材料になる
- **`/health-check` の参照パス実在確認のスクリプト化 — #213 で追跡中**（誤検出は較正で解消済みだが、書き捨ての検査器は毎サイクル同じ較正を要求する。較正を忘れれば偽の ⚠️ が本物の乖離を埋める）
