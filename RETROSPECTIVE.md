# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: 読み経路性能の段階修正（epic #140 / PR #157〜#162・#135 #136 #137 #156）

### 何をしたか

ベースライン実測（#133）を検収基準に、読み経路（検索・ソート・ページング・集計）の性能問題を統合設計書（PR #157）で 2 軸（物理設計とクエリ形状の不一致／クエリ発行規律の欠如）×4 フェーズに束ね、独立マージ可能な PR で段階修正した。Phase 0: 検収ハーネスの CI ガード化（PR #158）。Phase 1: クエリ発行規律 — COUNT 併走の解消・空クエリ分岐・EXISTS 化・RANDOM() 廃止（PR #159）。Phase 2: 候補形状の実測比較（PR #160）を経て search_text 生成列＋同乗 index へ載せ替え、検索最悪 305.6ms→16.8ms（PR #161）。FTS5 は実測とクエリ長分析（1〜2 文字で LIKE fallback）に基づき不採用。Phase 3: backfill_aggregates を cache miss 経路に限定し、Layer 1 高速パスの O(全起動履歴) GROUP BY を解消（PR #162）。Phase 4: 再判定ゲートの再実測で offset/deep 14.05ms < 16ms を確認し、#137 を意図的スキップとしてクローズ（実測と再浮上時のレバーは #137 コメントに記録）。

---

### よかったこと

**計測基盤を先に CI ガード下へ置き、同じ道具で全フェーズを検収した**
Phase 0 で bench ハーネスをコンパイル確認＋ガードテストとして CI に固定してから工事を始めた。設計時の想定を実測が二度覆した（FTS5 前提 → trigram の 3 文字制約で不採用、OFFSET ~4ms 想定 → 再実測 14ms）が、どちらも「古い実測や予測でなく、判定時点の再実測で裁定する」運用が機能したことで誤った設計に進まなかった。予測値は工事自体（Phase 2 の index 太化）で陳腐化する——ゲートは再実測で判定する。

**発行規律（JS のみ）と物理設計（スキーマ）を別軸に分離し、リスクの小さい方を先行させた**
Phase 1 はスキーマ不変で COUNT 増幅（体感の過半）を解消し、Phase 2 のスキーマ変更（全ユーザー自動リビルド＝受容済みコスト）は 1 回の改訂に束ねた。「index を足しても解けない問題」を先に分離したことで、Phase 2 の実測比較が発行規律のノイズなしで行えた。

**「発行されないこと」を観測ベースでテスト固定した**
Phase 3 で「Layer 1 hit で GROUP BY が発行されない」を、SQL トレース（グローバル状態が必要）でなく「backfill が走れば必ず集計列が書き換わる状況を seed してから、集計列の不変を assert する」形で証明した。副作用の不在は、副作用が必ず観測面に現れる状況を作れば KISS に固定できる。hit×2（非発行）＋miss×1（発行維持）の 3 経路対称ガードで将来の双方向の崩れも検知する。

**ページング方式の裁定で SQL 層だけでなく UI のアクセスパターンまで確認した**
Phase 4 の keyset 判定で、useVirtualizedList が scrollTop→絶対 index のランダムアクセス構造であることを確認してから裁定した。keyset は「次ページ」しか取れず任意ジャンプに OFFSET が残るため、この UI では部分適用にしかならない——ページング方式は SQL とUI アクセスパターンのセットで決まる。

---

### 伸びしろ

**設計書内の issue クローズ指示が PR フローに配線されず、クローズ漏れが起きた**
設計書 §4.1 は「#135 は Phase 2 完了時にクローズ」と明記していたが、PR #161 の本文に `Closes` が書かれず、#135/#136 は epic 整理（Phase 4 ゲート後）まで open のまま残った。文書内のクローズ指示は機械検証されない「規範」であり、PR 本文の `Closes #N` が唯一の自動化点。→ `/pr` ステップ 2 に「完了させる issue の洗い出しと Closes 配線」を追加（抽出済み）。

**index を太らせる変更の副作用を、狙った形状の検収だけでは検出できなかった**
Phase 2 の検収実測は狙った形状（search/sort）で行い受け入れ基準を満たしたが、同じ index を線形に歩く OFFSET 形状が 4ms→14ms に太っていたことは Phase 4 のゲート再実測まで気づかなかった（search_bench に offset 形状は常設されていたのに、回して差分を見る規律がなかった）。→ `/cache-check` 検査 4 に「index 追加・列同乗時は既存形状含む bench 全形状の再実測」を追加（抽出済み）。

---

### ネクストアクション

- **docs/locale-customization.md の複数形キー記載ギャップ — issue 化して追跡**（/health-check 検出・本サイクル起因ではない既存 drift）: キー一覧の `list.count` は日本語のみで、英語等は i18next 複数形キー `list.count_one`/`list.count_other` を使う。ユーザーが英語の件数表示を `list.count` で上書きしても効かない
- **FS 監視（ReadDirectoryChangesW）によるスキャン層の追加最適化 — 意図的に追跡しない**（設計書スコープ外の再確認）: 現行の 2 層 fingerprint で実用十分。ユーザー報告があれば再訪する
- **#137 の再浮上条件 — issue コメントに記録済み**: 遅い環境での 16ms 超過報告が出たら、第一手は痩せ sort index 並設（#137 コメント参照）
