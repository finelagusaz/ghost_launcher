# 振り返り

> 上書き更新。前回サイクルの内容は残さない。

## サイクル: Rust 1.98 移行と、そこから派生した依存更新（PR #202 / #204 / #205）

### 何をしたか

ローカル Rust toolchain を stable 1.97.1 → 1.98.0 へ上げ、機能への影響を検証した。toolchain 更新自体はリポジトリに差分を生まない（CI が `dtolnay/rust-toolchain@stable` の浮動 stable のため）が、検証の過程で三つの成果物が出た。PR #202: 検証されないまま残っていた `rust-version` 宣言を実測値へ是正。PR #204: レンジ内の依存更新（npm 直接依存 9 件・cargo 63 件更新）。PR #205: Fluent UI の ESM-first 化に追随する vitest 設定。あわせて `cargo bench` の順序依存ビルド失敗（#201）と、Fluent UI の ESM 破綻（#203、PR #205 で解決）を起票した。

---

### よかったこと

**赤の帰属を担保する順序設計を、前サイクルの教訓どおり再現できた**
1.97.1 で無変更ツリーのベースラインを取り、同一ゲートを `cargo +1.98` で回してから既定を切り替えた。テスト本数まで一致したため、以降に出た赤はすべて 1.98 以外に帰属できた。前サイクルで抽出した規律が `/deps-update` に載っていたことで、判断を毎回作り直さずに済んだ。**教訓を単一権威へ配る運用が実際に効いた事例**である。

**計器を較正したことで、25 件の「有意な性能悪化」を退けられた**
1.97.1 と 1.98 の A/B で n=100k 群が一律 +7〜12% 悪化し、`p = 0.00` が並んだ。しかし悪化が benchmark の**形状**でなく**実行順序**に沿っており、n=10k 群の途中で段差が出ていた。同一バイナリを 2 回連続で回す較正を挟んだところ、−28.5%・`p = 0.00` の「有意な改善」が出た。**p 値が前提とする反復条件がこの環境で成立していない**ことが測れたため、A/B の差はノイズと判定できた。較正を省いていれば、存在しない regression を報告していた。

**矛盾する出力から推論せず、再現条件を実測で確定した**
`scan_bench` が両フェーズとも落ちる一方で同一設定の `search_bench` は通る、という食い違いに当たった。ここで機序を推測せず単独実行を試し、`search_bench` の後に呼んだときだけ落ちるという順序依存を確定させた。**再現条件が確定してから起票した**ため、#201 は推測でなく実測で書けている。

**計器の視界の欠損を、対象の不在と読まなかった**
`Cargo.lock` に 10 件のクレートが増えたが `cargo tree -i` は `--target all` でも何も返さなかった。ここで「存在しない」と結論せず lock を直接パースし、`serde_with` の optional な `jiff` 統合が出所だと突き止めた。**lock と `cargo tree` の食い違いそのものが答え**であり、どちらか一方だけでは判定できなかった。

**原因の帰属を、想定でなく実測で証明した**
Fluent UI の更新で 9 テストファイルが落ちた際、当該 1 件のみを据え置き他をすべて適用した状態で全通過することを確認してから見送りを決めた。帰属が証明済みのため、PR #204 の据え置き判断と #203 の記述に推測が混じっていない。

---

### 伸びしろ

**上流の変更が意図的かを確かめず「上流の不備」と issue に書いた**
#203 に当初「対応案 1: 上流の修正を待つ」と書いた。ユーザーの指摘を受けて changelog を読むと、ESM-first 化は数十パッケージ一斉の設計判断で、`drop the node export condition` と名指しされていた。**意図的な変更は待っても直らないため、「修正を待つ」は最初から選択肢ではなかった**。changelog を先に読めば避けられた誤りであり、指摘がなければ誤った前提の記録が残っていた。→ `/deps-update` ステップ 2 へ抽出済み。

**較正の教訓を、自分の副次的な計測に適用しそこねた**
criterion のノイズを実測で暴いた直後に、inline 設定の実行時間を単発計測で比較し「遅くならない」と述べた。同条件の再計測で 19.50s → 34.44s と倍近く動き、主張を撤回した。**性能計測に適用した規律が、性能を主題としない場面の数字には適用されていなかった**。「単発の数字で速い/遅いを言わない」は計測の主題によらず効く。→ ルート CLAUDE.md「判断の勘所」へ抽出済み。

**A/B の順序バイアスを、計測設計に織り込めなかった**
機械を専有させる配慮まではしたが、フェーズ 1 → 2 という固定順序が結果に乗ることを事前に想定していなかった。較正で事後に気づけたのは幸運に近い。**同一条件の反復が成り立つかは、実行順序も含めて設計時に問う**べきだった。→ 上記の CLAUDE.md 追記に含めた。

**ドキュメント上の規約が実態から乖離していたことを、規約に従おうとして初めて気づいた**
ブランチ命名規約は `chore/` を認めておらず issue 番号を必須と読める書き方だったが、直近 10 件のマージ済みブランチのうち `chore/` が 3 件、issue 番号を持たないものが 4 件あった。`/commit` は commit prefix に `chore:` を認めており内部でも不整合だった。**規約に従おうとした側が違和感を持たなければ、乖離は検出されないまま残る**。→ ルート CLAUDE.md「ブランチ戦略」を実態へ是正済み。

---

### ネクストアクション

- **`cargo bench` の順序依存ビルド失敗と、その検知経路の不在 — #201 で追跡中**。`docs/perf/baseline-100k.md` の実行手順どおりになぞると踏む。あわせて `/health-check` 項目 6 で検出した ⚠️（既存の bench ゲートは dev プロファイルの `cargo check` 止まりで、`panic = "abort"` を持つ release 継承の bench プロファイルをコンパイルする経路が CI・チェックリストのどちらにも無い）を、同 issue の受け入れ基準へ吸収した。原因と検知盲点が同じ根を持ち、別建てにすると両方を見ながら直すことになるため一本化した
- **`rusqlite` の上流追従 — #187 で追跡中**（`tauri-plugin-sql` の sqlx 0.9 対応待ち。解錠条件は未達）
- **`jsdom` 30 / `sha2` 0.11 のメジャー更新 — #198 / #199 で追跡中**
- **Fluent UI の inline 回避策の撤去 — 意図的に追跡しない**: 撤去条件と判定コマンド、上流の追跡先（`microsoft/fluentui#34685`）は `vitest.config.ts` のコメントに記録済みで、そちらが単一権威。引き金は上流 issue の close であり、こちらから定期監視する価値は低い。次に Fluent UI を更新する担当者が判定コマンドを実行すれば足りる
- **`docs/perf/baseline-100k.md` の数値の扱い — 意図的に追跡しない**: 本サイクルで計測系のばらつきが ±28% 規模と判明したが、同ドキュメントが導く結論は倍率（`like/none` が index 経路の約 3662 倍など）であり、この規模のノイズでは覆らない。細かい絶対値比較に使わない限り改訂の必要はなく、計測を信じる前の較正はルート CLAUDE.md 側の規律として書いた
